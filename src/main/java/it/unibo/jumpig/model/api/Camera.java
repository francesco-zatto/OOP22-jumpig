package it.unibo.jumpig.model.api;

/**
 * Interface Camera.
 */
public interface Camera {
    /**
     * Getter that returns the camera's height.
     * @return an Integer that represents the camera's height
     */
    Integer getHeight();
}
